import Layout from '@/components/layout';

export default function CreateStream() {
  return (
    <Layout title="Create Live Stream" canGoBack hasTabBar={false}>
      <div>Create</div>
    </Layout>
  );
}
